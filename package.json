{
  "name": "server-apollo-express",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon src/index --watch src --exec babel-node",
    "build": "babel src -d dist"
  },
  "dependencies": {
    "apollo-server-express": "^2.0.7",
    "express": "^4.16.3",
    "graphql": "^14.0.2",
    "graphql-tools": "^3.1.1",
    "lodash.merge": "^4.6.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "cross-env": "^5.2.0",
    "nodemon": "^1.18.4"
  }
}
